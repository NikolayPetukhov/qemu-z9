how to compile:

./configure --target-list=arm-softmmu
make

How to run lk bootloader:

./arm-softmmu/qemu-system-arm -M ztev9 -option-rom appsboot.mbn -serial stdio


How to run linux kernel:

./arm-softmmu/qemu-system-arm -M ztev9 -kernel zImage -serial stdio

